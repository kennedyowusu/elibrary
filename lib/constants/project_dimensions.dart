import 'package:get/get.dart';

class ProjectDimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

// Containers
  static double pageViewContainer = screenHeight / 3.55;
  static double pageViewSubContainer = screenHeight / 11.16;
  static double pageViewMainContainer = screenHeight / 2.79;
  static double pageViewMainContainerWidth = screenWidth / 2.79;

  // Shoe Page Container
  static double shoePageContainer = screenHeight / 17.36;

  static double heightFortyFive = screenHeight / 17.36;
  static double widthFortyFive = screenWidth / 9.33;

  static double heightTen = screenHeight / 78.1;
  static double heightTwenty = screenHeight / 39.05;
  static double widthTen = screenWidth / 78.1;
  static double widthTwenty = screenWidth / 39.05;
  static double paddingWidthTwenty = screenWidth / 39.05;
  static double paddingHeightTwenty = screenHeight / 39.05;

  static double sizedBoxHeightTen = screenHeight / 78.1;

  static double sizedBoxWidthTen = screenWidth / 78.1;
  static double sizedBoxWidthTwenty = screenWidth / 39.05;

  // Padding
  static double paddingFive = screenHeight / 156.2;
  static double paddingTen = screenHeight / 78.1;
  static double paddingFifteen = screenHeight / 52.07;
  static double paddingTwenty = screenHeight / 39.05;
  static double paddingTwentyFive = screenHeight / 31.24;
  static double paddingThirty = screenHeight / 26.03;

  // Text
  static double textSizeTen = screenHeight / 78.1;
  static double textSizeTwenty = screenHeight / 19.53;
  static double textSizeTwelve = screenHeight / 68.08;
  static double textSizeEighteen = screenHeight / 43.39;
  static double textSizeFifteen = screenHeight / 52.07;

  // Radius
  static double radiusTen = screenHeight / 78.1;
  static double radiusTwenty = screenHeight / 39.05;

  // Icon Size
  static double iconSizeTen = screenHeight / 78.1;
  static double iconSizeFifteen = screenHeight / 52.07;
  static double iconSizeEighteen = screenHeight / 43.39;
  static double iconSizeTwenty = screenHeight / 39.05;
  static double iconSizeTwentyFour = screenHeight / 34.54;

  // ListView
  static double listViewImageContainerHeight = screenHeight / 6.51;
  static double listViewTextContainerHeight = screenHeight / 8.68;

  // Detail Screen
  static double detailScreenContainerHeight = screenHeight / 2.79;
  static double detailScreenBottomContainerHeight = screenHeight / 7.10;

  // splash screen
  static double splashScreenContainerHeight = screenHeight / 3.905;

  static double splashScreenContainerWidth = screenWidth / 1.96;
}
